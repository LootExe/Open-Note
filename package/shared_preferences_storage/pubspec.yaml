name: shared_preferences_storage
description: A Shared Preferences implementation of the StorageProvider interface
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.2.1
  storage_provider:
    path: ../storage_provider

dev_dependencies:
  flutter_test:
    sdk: flutter

  mocktail: ^1.0.0
  very_good_analysis: ^5.1.0
