name: local_note_provider
description: An implementation of the note provider using shared storage.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  meta: ^1.8.0
  shared_preferences: ^2.0.15
  note_provider:
    path: ../note_provider

dev_dependencies:
  test: ^1.22.0